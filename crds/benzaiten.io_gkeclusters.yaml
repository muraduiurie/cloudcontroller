---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: gkeclusters.benzaiten.io
spec:
  group: benzaiten.io
  names:
    kind: GKECluster
    listKind: GKEClusterList
    plural: gkeclusters
    shortNames:
    - gke
    singular: gkecluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: GKECluster is the Schema for the gkeclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of GKECluster
            properties:
              initialNodeCount:
                description: InitialNodeCount is the number of nodes to create in
                  the GKE cluster
                format: int32
                type: integer
              name:
                description: Name is the name of the GKE cluster
                type: string
            required:
            - initialNodeCount
            - name
            type: object
          status:
            description: Status defines the observed state of GKECluster
            properties:
              phase:
                description: Phase is the current state of the GKE cluster
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
